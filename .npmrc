# Keep plain `npm install` working when `three` is pinned to the MaterialX fork
# via a GitHub tarball. Spark's peer resolution treats this dependency form as
# versionless during install, even though the installed package reports a recent
# Three.js version, so we keep the repo-local override checked in.
legacy-peer-deps=true
